# 피치CRM UI 프로토타입 - Claude 에이전트 지침

> 이 문서는 Claude Code / Cursor Agent가 이 프로젝트에서 작업할 때 따라야 할 핵심 지침이다.

---

## 최우선 규칙

1. **한국어 응답**: 모든 응답과 코드 주석은 한국어로 작성
2. **`AGENTS.md` 필수 참조**: UI 관련 작업 시 반드시 `AGENTS.md`를 먼저 읽고 해당 규칙을 따른다

---

## AGENTS.md 참조 가이드

`AGENTS.md`에 다음 내용이 모두 정의되어 있다. **중복 작성하지 않으며, 필요 시 해당 문서를 직접 참조하라.**

| 섹션 | 내용 |
|------|------|
| 1. 프로젝트 컨텍스트 | 시스템 구조, 모듈 구성, 필라테스 특화 기능 |
| 2. UI 프로토타입 생성 규칙 | **필수 스킬 3종** (gen-design, gen-ui, frontend-design), 적용 순서, 모듈별 UI 가이드 |
| 3. 코드 생성 표준 | 파일 구조, 가이드 코드 참조, 필수 코드 패턴, 컴포넌트 선택 규칙 |
| 4. 디자인 시스템 | 색상, 타이포그래피, 간격, 다크모드 |
| 5. 프로토타입 특화 규칙 | Mock 데이터, 라우팅, 좌측 메뉴 구조 |
| 6. 검증 체크리스트 | UI 작업 완료 시 확인 항목 |

```
📌 UI 작업 시작 전: AGENTS.md를 먼저 읽고 섹션 2의 스킬 적용 순서를 반드시 따를 것
   요청 수신 → gen-design(방향) → gen-ui(생성) → frontend-design(검증) → 빌드 확인
```

---

## 참조 문서 우선순위

| 순위 | 문서 | 용도 |
|------|------|------|
| **1** | `AGENTS.md` | UI 프로토타입 생성 규칙 (가장 중요) |
| **2** | `.claude/skills/gen-design/SKILL.md` | 디자인 시스템 스킬 |
| **3** | `.claude/skills/gen-ui/SKILL.md` | UI 생성 스킬 |
| **4** | `.cursor/rules/*.mdc` | Cursor IDE 개발 가이드라인 |
| **5** | `.cursorrules` | 프로젝트 루트 규칙 |
| **6** | `src/modules/test-data/` | 가이드 코드 (패턴 참조) |
| **7** | `crm-ai-dev/prd/` | PRD 문서 (요구사항) |
